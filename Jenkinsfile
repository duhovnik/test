pipeline {
    agent any
    }

    stages {
        stage('Hello') {
            steps {
                sh '''
                echo Launching script
                bash info.sh
                '''
            }
        }
    }
}
